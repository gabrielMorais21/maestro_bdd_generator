name: maestro_bdd_generator
description: CLI that converts Gherkin .feature scenarios into Maestro .yaml test templates with smart merge support.
version: 1.0.2
repository: https://github.com/gabrielMorais21/maestro_bdd_generator

environment:
  sdk: ^3.11.5

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  maestro_bdd: maestro_bdd_generator