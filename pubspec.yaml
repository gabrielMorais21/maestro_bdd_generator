name: maestro_bdd_generator
description: A sample command-line application.
version: 1.0.0
repository: https://github.com/diasgabriel/maestro_bdd_generator

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

# É AQUI QUE A MÁGICA ACONTECE
executables:
  # O comando que o usuário vai digitar: O arquivo dentro da pasta bin/ (sem o .dart)
  maestro_bdd: maestro_bdd_generator